name: Deploy Juego Enigma Madrid (Producción)

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy via SFTP
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Install lftp
        run: sudo apt-get install -y lftp

      - name: Limpiar servidor remoto
        run: |
          lftp -u "${{ secrets.PROD_FTP_USERNAME }}","${{ secrets.PROD_FTP_PASSWORD }}" \
            -e "set sftp:connect-program \"ssh -a -x -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null\"; \
                set net:timeout 30; \
                set net:max-retries 2; \
                glob rm -rf ./*; \
                quit" \
            sftp://${{ secrets.PROD_FTP_SERVER }}:22

      - name: Deploy via SFTP
        run: |
          lftp -u "${{ secrets.PROD_FTP_USERNAME }}","${{ secrets.PROD_FTP_PASSWORD }}" \
            -e "set sftp:connect-program \"ssh -a -x -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null\"; \
                set net:timeout 30; \
                set net:max-retries 2; \
                set net:reconnect-interval-base 5; \
                mirror --reverse \
                  --exclude-glob .git/ \
                  --exclude-glob .github/ \
                  --exclude-glob .vscode/ \
                  --exclude-glob .DS_Store \
                  --exclude-glob README.md \
                  --exclude-glob .gitignore \
                  --exclude-glob backup-30-01-2026/ \
                  --exclude-glob test-deploy.html \
                  ./ ./; \
                quit" \
            sftp://${{ secrets.PROD_FTP_SERVER }}:22
